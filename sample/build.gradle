dependencies {
    annotationProcessor(
            project(':javac-string-replace-plugin')
    )
    compileOnly(
            project(':javac-string-replace-plugin')
    )
}

compileJava {
    options.compilerArgs += ['-Arip.deadcode.javac.stringreplace.properties=version=0.1.0']
}
